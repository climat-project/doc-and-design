```clidsl title=toolchain.cli {3-4}
my-toolchain {

    @aliases(ct child) // Defines one or more aliases
    @alias(c)          // Defines one alias
    sub child-toolchain {
        action "echo 'Child'"
    }

}
```